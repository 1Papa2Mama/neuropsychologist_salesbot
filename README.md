# 🤖 Нейропсихолог - ИИ-Продажник Telegram Bot

Этот проект представляет собой полностью рабочий Telegram-бот, который выступает в роли продающего консультанта для проекта **«Нейропсихолог»** (Цифровой Психолог).

Бот использует архитектуру **RAG (Retrieval-Augmented Generation)** для предоставления точных ответов на основе предоставленной базы знаний (информации с лэндинга) и использует модель GPT для генерации дружелюбных, продающих ответов.

## 🚀 Функционал

*   **ИИ-Продажник:** Дружелюбный и эмпатичный консультант.
*   **База Знаний:** Использует информацию с лэндинга для ответов.
*   **Консультации:** Отвечает на вопросы о психологии, тревогах, проблемах и страхах.
*   **Продажи:** Мягко мотивирует пользователя к покупке проработки, используя ключевые продающие акценты.
*   **Автоматические ссылки:** Предоставляет ссылки на продукт-бот и лэндинг.
*   **Приветствие:** Обрабатывает команду `/start` с теплым приветствием и описанием проекта.

## 🛠️ Архитектура

*   **Язык:** Python
*   **Фреймворк:** `aiogram` (для работы с Telegram API)
*   **ИИ:** `openai` (для генерации ответов с использованием GPT)
*   **Подход:** RAG (Retrieval-Augmented Generation)

## ⚙️ Установка и Запуск (Локально)

### 1. Клонирование репозитория

```bash
git clone <URL_ВАШЕГО_РЕПОЗИТОРИЯ>

```

### 2. Создание виртуального окружения и установка зависимостей

```bash
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
```

### 3. Настройка переменных окружения

Создайте файл `.env` в корневой папке проекта и добавьте в него ваши токены:

```
TELEGRAM_TOKEN="ВАШ_TELEGRAM_API_ТОКЕН"
OPENAI_API_KEY="ВАШ_OPENAI_API_КЛЮЧ"
```

**Внимание:** Токены, указанные в файле `config.py`, являются тестовыми. Для запуска бота необходимо использовать свои рабочие токены.

### 4. Запуск бота

Для локального запуска (Long Polling):

```bash
python bot.py
```

## ☁️ Развертывание на Render (WebHook)

Для развертывания на Render необходимо использовать WebHook.

### 1. Создание Web Service

1.  Зайдите в панель управления Render и создайте новый **Web Service**.
2.  Подключите ваш GitHub-репозиторий с кодом бота.
3.  **Environment:** Python 3.
4.  **Build Command:** `pip install -r requirements.txt`
5.  **Start Command: python bot.py`

### 2. Настройка переменных окружения в Render

В настройках Web Service добавьте следующие переменные окружения:

| Ключ | Значение | Описание |
| :--- | :--- | :--- |
| `TELEGRAM_TOKEN` | `ВАШ_TELEGRAM_API_ТОКЕН` | Токен, полученный от BotFather. |
| `OPENAI_API_KEY` | `ВАШ_OPENAI_API_КЛЮЧ` | Ваш ключ для доступа к GPT. |
| `WEBHOOK_HOST` | `https://<ИМЯ_ВАШЕГО_СЕРВИСА>.onrender.com` | URL вашего сервиса на Render (берется из настроек). |
| `PORT` | `10000` | Порт, который будет слушать Web Service (Render часто использует 10000). |

### 3. Проверка работоспособности

После успешного деплоя Render автоматически запустит команду `python bot.py`. Благодаря логике в `bot.py`, бот переключится в режим WebHook, установит его и начнет принимать сообщения.

**Для проверки:**
1.  Откройте лог Web Service в Render. Вы должны увидеть сообщение о запуске сервера на порту.
2.  Отправьте команду `/start` вашему боту в Telegram.
3.  Бот должен ответить приветственным сообщением.

---

**Разработано Manus**
